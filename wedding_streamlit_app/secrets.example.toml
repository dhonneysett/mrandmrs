# Copy this file to .streamlit/secrets.toml (DO NOT commit secrets.toml to GitHub)
ADMIN_PASSWORD = "WED-UPZWJHHI6Z"

# OPTIONAL (recommended before sending real invites):
# Set STORAGE_MODE = "local" for testing only.
# Set STORAGE_MODE = "gcsheets" once you wire up Google Sheets.
STORAGE_MODE = "local"

# ---- Google Sheets (optional) ----
# GCP_SERVICE_ACCOUNT_JSON = '''{ ... full json ... }'''
# SHEET_ID = "your_google_sheet_id"
